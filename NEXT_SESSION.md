# 오름 다음 작업 인수인계

작성일: 2026-09-04

## 프로젝트 목적

학생과의 기본 질문을 통해 목표, 현재 수준, 사용할 수 있는 시간과 학습 성향을 파악하고,
해야 할 공부를 실제 가능한 시간에 자동 배치하는 수능 리듬 기반 학습 스케줄러다.

핵심 흐름:

```text
대화형 기본 질문
→ 학생 프로필 저장
→ 공부할 일 등록
→ 규칙 기반 일일 계획 생성
→ 수행 결과 기록
→ 다음 계획 보정
```

Gemini API는 현재 사용하지 않는다. 초기 버전은 구조화된 질문과 규칙 기반 스케줄러로 동작한다.

## 현재 구현 상태

### 대화형 기본 질문

- 목표 종류와 구체적인 목표
- 목표 날짜
- 현재 수준
- 관리할 과목
- 취약 과목
- 학교 종료 시간과 학원 등 고정 일정 메모
- 수면시간
- 평일 공부 가능 시간
- 주말 공부 가능 시간
- 집중·휴식 주기
- 계획 강도
- 최종 요약 확인 및 저장

답변은 `backend/data/profile.json`에 저장된다.

### 학습 작업 관리

- 과목, 제목, 예상시간, 목표일, 우선순위, 메모 등록
- 일부 완료시간 기록
- 완료 처리와 되돌리기
- 작업 삭제
- 전체 작업 진행률 계산

작업은 `backend/data/tasks.json`에 저장된다.

### 자동 스케줄러

- 선택 날짜의 공부 가능 시간 계산
- 계획 강도 70%, 85%, 95% 반영
- 우선순위, 목표일, 취약 과목 반영
- 집중시간 단위로 작업 분할
- 세션 사이 휴식시간 반영
- 각 세션의 배치 이유 표시
- 배치하지 못한 작업 이월 안내

스케줄러 구현 파일은 `backend/app/scheduler.py`다.

### 화면 구조

- `홈`
  - 목표와 실제 진행률
  - 오늘의 자동 계획
  - 수능형 학습 흐름
  - 자세 코치 자리
  - 주간 학습 습관
- `공부 관리`
  - 학습 작업 등록 및 진행 관리
- `기본 설정`
  - 프로필 요약
  - 대화형 질문 다시 진행

## 확인된 테스트 상태

- 백엔드 테스트 4개 통과
- 프론트엔드 `npm run lint` 통과
- 프론트엔드 `npm run build` 통과

## 다음 작업 우선순위

### 1. 고정 일정 구조화

현재 학원 일정은 `fixed_schedule_notes`라는 문장으로만 저장된다.
자동 스케줄러가 시간 충돌을 계산할 수 있도록 다음 구조로 변경한다.

```json
{
  "title": "수학 학원",
  "days": ["화", "목"],
  "start_time": "19:00",
  "end_time": "21:00"
}
```

- 요일 복수 선택
- 시작·종료 시간 입력
- 일정 추가·수정·삭제
- 학교, 학원, 식사 등 일정 유형 구분
- 자동 계획에서 고정 일정 시간 제외

### 2. 생성된 계획 저장

현재 계획은 화면 메모리에만 존재하고 새로고침하면 사라진다.

- `backend/data/plans.json` 또는 날짜별 JSON 저장
- 생성한 계획 다시 불러오기
- 계획 확정 여부 저장
- 과거 날짜 계획 조회
- 계획 다시 생성 시 기존 계획 처리 방식 결정

### 3. 계획 직접 편집

- 세션 순서 변경
- 시작 시간 변경
- 오늘 계획에서 제외
- 다른 날짜로 이동
- 계획 다시 만들기
- 사용자가 변경한 결과를 저장

### 4. 실행 및 완료 기록

- 계획 세션별 시작 버튼
- 타이머
- 완료, 일부 완료, 미실행 선택
- 실제 집중시간 기록
- 완료한 분량 입력
- 중단 이유 선택

### 5. 다음 계획 자동 보정

- 계획시간과 실제시간 비교
- 과목·활동별 실제 수행 속도 계산
- 미완료 작업 다음 날짜 재배치
- 과도한 일일 계획 감축
- 예상시간 자동 보정

### 6. 실제 통계로 교체

현재 주간 습관 영역은 완료 작업 개수를 단순 표시한다.

- 날짜별 공부시간
- 계획 달성률
- 과목별 학습시간
- 연속 학습일
- 수능 시간대 학습률
- 적응도 산출 근거 표시

### 7. 자세·졸음 감지는 이후 진행

자동 계획과 수행 기록이 안정화된 후 구현한다.

- 사용자 동의 후에만 카메라 실행
- 원본 영상 저장 금지
- 브라우저 또는 기기 내부 처리
- 자세·졸음 감지를 의료 판단으로 표현하지 않음
- 감지 결과를 실제 집중 기록과 연결

## 실행 방법

### 새 라즈베리파이 5 최초 환경 구축 (원클릭)

```bash
./setup.sh
```

### 백엔드/프론트엔드 동시 실행 (원클릭)

```bash
./start.sh
```

접속 주소:
- 로컬 브라우저: `http://localhost:5173` 또는 `http://127.0.0.1:5173`
- 같은 Wi-Fi의 다른 기기/스마트폰: `http://<라즈베리파이IP>:5173`
- 백엔드 API 문서: `http://<라즈베리파이IP>:8100/docs`

(개별 수동 실행 필요 시: 백엔드는 `backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8100`, 프론트엔드는 `frontend`에서 `npm run dev -- --host 0.0.0.0`)

### 새 라즈베리파이 이전용 압축 패키징

```bash
./package_project.sh
# ../study-rhythm-coach.tar.gz 생성
```

## 다음 세션 시작 요청 예시

```text
NEXT_SESSION.md를 읽고 오름 개발을 이어서 진행해 줘.
먼저 고정 일정을 구조화하고 자동 계획에서 학교·학원 시간과 충돌하지 않도록 수정해 줘.
기존 홈, 공부 관리, 기본 설정 탭과 저장된 사용자 데이터는 유지해 줘.
```
